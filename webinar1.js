//Codificando o jantar de hoje
const alimentoSelecionado = "pedra";

//Modo 1
switch (alimentoSelecionado) {
    case "arroz":
        console.log ("vamos jantar " + alimentoSelecionado);
        break;

    case "batata":
        console.log ("vamos jantar " + alimentoSelecionado)
        break;

    case "lasanha":
        console.log ("vamos jantar " + alimentoSelecionado)
        break;
        
    default:
        console.log ("o item não consta na lista")
        break;
        
}