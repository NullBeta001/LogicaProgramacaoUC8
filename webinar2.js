//Codificando o jantar de hoje
const alimentoSelecionado = "arroz";

//Modo 2


if (alimentoSelecionado === "arroz") {
    console.log ("vamos jantar " + alimentoSelecionado);
} else if (alimentoSelecionado === "lasanha") {
    console.log ("vamos jantar " + alimentoSelecionado);
} else if (alimentoSelecionado === "batata") {
    console.log ("vamos jantar " + alimentoSelecionado);
} else {
    console.log ("o item não consta na lista")
}